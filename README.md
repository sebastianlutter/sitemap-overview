# Simple sitemap scanner

## Usage
* Clone repository
* Create venv
```
cd sitemap-scanner
python3 -mvenv .
source bin/activate
```
* Install requirements
```
pip3 install --upgrade pip
pip3 install -r requirements.txt
```
